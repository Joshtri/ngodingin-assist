// Temporarily use local fonts to avoid network issues during development
export const fontSans = {
  variable: "--font-sans",
  className: "font-sans",
};

export const fontMono = {
  variable: "--font-mono", 
  className: "font-mono",
};
